// QuizPage was replaced by QuestionListPage + QuestionDetailPage in feature 002-question-list-navigation.
import { describe } from 'vitest';

describe.skip('QuizPage (removed)', () => {
  // Tests removed — component replaced by QuestionListPage and QuestionDetailPage.
});
