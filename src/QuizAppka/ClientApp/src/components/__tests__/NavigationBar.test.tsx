// NavigationBar was removed in feature 002-question-list-navigation.
// Navigation is now handled via the question list page.
import { describe } from 'vitest';

describe.skip('NavigationBar (removed)', () => {
  // Tests removed — component no longer exists.
});
